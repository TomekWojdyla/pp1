def j(years, course):
    import json
    file = open("data.json")
    file_content = json.loads(file)
    lent = len(file_content)
    for 
    for key, value in file_content:
        if key =="age":
            if value>=years:
                record = {key}
                
    
    
    file.close()
    return count